{
  "name": "kaervax",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Small, public-safe KAERVAX canonical domain foundation",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "rm -rf dist && cp -R public dist && node scripts/verify-build.mjs",
    "test": "node --test",
    "verify": "npm run build && npm test",
    "preview": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "deploy": "npm run verify && wrangler pages deploy dist --project-name kaervax"
  },
  "devDependencies": {
    "wrangler": "^4.37.1"
  }
}
