# KAERVAX Phase 1 application runtime
# No application environment variables are required for the static foundation.
#
# Deployment authentication is supplied outside the repository through the
# approved Cloudflare BYOK mechanism. Never place real credentials here.
